/**
 * Default play state enum
 */
export enum EPlayState {
  HIDDEN,
  PLAY_OUT,
  PLAY_IN,
  VISIBLE
}

/**
 * Env names list
 */
export enum EEnv {
  DEV = "development",
  STAGING = "staging",
  PROD = "production"
}
